<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lottery Receipt - <%= receiptData.receiptNumber %></title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #1e3a5f 0%, #2c5282 100%);
            min-height: 100vh;
            padding: 25px 15px;
            color: #333;
            font-size: 18px;
        }

        .receipt-container {
            max-width: 900px;
            margin: 0 auto;
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            overflow: hidden;
            animation: slideUp 0.6s ease-out;
        }

        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .receipt-header {
            background: linear-gradient(135deg, #1e3a5f 0%, #2c5282 100%);
            color: white;
            padding: 40px 30px;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .receipt-header::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255, 255, 255, 0.1) 0%, transparent 70%);
            animation: pulse 4s ease-in-out infinite;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); opacity: 0.5; }
            50% { transform: scale(1.1); opacity: 0.8; }
        }

        .receipt-header h1 {
            font-size: 3rem;
            font-weight: 800;
            margin-bottom: 15px;
            position: relative;
            z-index: 1;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2);
            letter-spacing: 1px;
        }

        .receipt-number {
            font-size: 1.5rem;
            font-weight: 600;
            opacity: 0.95;
            position: relative;
            z-index: 1;
            letter-spacing: 2px;
        }

        .receipt-body {
            padding: 40px 30px;
        }

        .info-section {
            margin-bottom: 40px;
            padding: 30px;
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            border-radius: 15px;
            border-left: 5px solid #ff6b35;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.08);
        }

        .info-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 18px 0;
            border-bottom: 1px solid rgba(0, 0, 0, 0.08);
            flex-wrap: wrap;
        }

        .info-row:last-child {
            border-bottom: none;
        }

        .info-label {
            font-weight: 700;
            color: #1e3a5f;
            font-size: 1.4rem;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .info-value {
            color: #333;
            font-weight: 600;
            text-align: right;
            font-size: 1.4rem;
        }

        .tickets-section {
            margin-top: 40px;
        }

        .section-title {
            font-size: 2rem;
            font-weight: 800;
            color: #1e3a5f;
            margin-bottom: 25px;
            padding-bottom: 15px;
            border-bottom: 4px solid #ff6b35;
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .ticket-card {
            background: white;
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 25px;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
            border: 2px solid #e9ecef;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .ticket-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.15);
            border-color: #ff6b35;
        }

        .ticket-card.winner {
            border: 3px solid #ffd700;
            background: linear-gradient(135deg, #fffbeb 0%, #fef3c7 100%);
            box-shadow: 0 8px 25px rgba(255, 215, 0, 0.3);
        }

        .ticket-card.winner::before {
            content: '🏆';
            position: absolute;
            top: 15px;
            right: 15px;
            font-size: 3rem;
            animation: trophy-bounce 2s ease-in-out infinite;
            filter: drop-shadow(0 4px 8px rgba(255, 215, 0, 0.5));
        }

        @keyframes trophy-bounce {
            0%, 100% { transform: translateY(0) rotate(0deg); }
            25% { transform: translateY(-10px) rotate(-5deg); }
            75% { transform: translateY(-10px) rotate(5deg); }
        }

        .ticket-card.winner::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: radial-gradient(circle at top right, rgba(255, 215, 0, 0.2) 0%, transparent 70%);
            pointer-events: none;
            animation: glow-pulse 3s ease-in-out infinite;
        }

        @keyframes glow-pulse {
            0%, 100% { opacity: 0.5; }
            50% { opacity: 1; }
        }

        .ticket-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            flex-wrap: wrap;
            gap: 15px;
        }

        .ticket-type {
            font-weight: 700;
            color: #ff6b35;
            font-size: 1.5rem;
            text-transform: uppercase;
            letter-spacing: 1.5px;
        }

        .winner-badge {
            background: linear-gradient(135deg, #ffd700 0%, #ffed4e 100%);
            color: #1e3a5f;
            padding: 8px 20px;
            border-radius: 25px;
            font-size: 1.1rem;
            font-weight: 800;
            box-shadow: 0 3px 10px rgba(255, 215, 0, 0.4);
            animation: badge-glow 2s ease-in-out infinite;
        }

        @keyframes badge-glow {
            0%, 100% { box-shadow: 0 3px 10px rgba(255, 215, 0, 0.4); }
            50% { box-shadow: 0 5px 20px rgba(255, 215, 0, 0.7); }
        }

        .ticket-details {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }

        .detail-item {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

        .detail-label {
            font-size: 1.1rem;
            color: #666;
            font-weight: 600;
        }

        .detail-value {
            font-size: 1.4rem;
            color: #1e3a5f;
            font-weight: 700;
        }

        .ticket-numbers {
            margin-top: 25px;
            padding-top: 25px;
            border-top: 2px dashed #ddd;
        }

        .numbers-label {
            font-size: 1.4rem;
            font-weight: 700;
            color: #1e3a5f;
            margin-bottom: 18px;
        }

        .numbers-grid {
            display: flex;
            flex-wrap: wrap;
            gap: 15px;
        }

        /* 🎯 TICKET NUMBERS - MAXIMUM ATTENTION GRABBING */
        .ticket-number {
            background: linear-gradient(135deg, #1e3a5f 0%, #2c5282 100%);
            color: white;
            padding: 20px 28px;
            border-radius: 15px;
            font-weight: 900;
            font-size: 2rem;
            min-width: 85px;
            text-align: center;
            box-shadow: 0 6px 20px rgba(30, 58, 95, 0.4);
            transition: all 0.3s ease;
            letter-spacing: 2px;
            border: 3px solid rgba(255, 255, 255, 0.1);
            position: relative;
        }

        .ticket-number::before {
            content: '';
            position: absolute;
            inset: -3px;
            border-radius: 15px;
            padding: 3px;
            background: linear-gradient(135deg, #ff6b35, #1e3a5f, #ff6b35);
            -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
            -webkit-mask-composite: xor;
            mask-composite: exclude;
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .ticket-number:hover {
            transform: scale(1.15) rotate(5deg);
            box-shadow: 0 10px 30px rgba(30, 58, 95, 0.6);
        }

        .ticket-number:hover::before {
            opacity: 1;
        }

        .ticket-card.winner .ticket-number {
            background: linear-gradient(135deg, #ff6b35 0%, #ff8c42 100%);
            box-shadow: 0 6px 20px rgba(255, 107, 53, 0.5);
            animation: number-shine 3s ease-in-out infinite;
            font-size: 2.2rem;
            font-weight: 900;
            border: 3px solid rgba(255, 215, 0, 0.5);
        }

        @keyframes number-shine {
            0%, 100% { 
                filter: brightness(1);
                transform: scale(1);
            }
            50% { 
                filter: brightness(1.3);
                transform: scale(1.05);
            }
        }

        .qr-section {
            margin-top: 45px;
            padding: 35px;
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            border-radius: 15px;
            text-align: center;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.08);
        }

        .qr-title {
            font-size: 1.6rem;
            font-weight: 700;
            color: #1e3a5f;
            margin-bottom: 20px;
        }

        #qrcode {
            display: inline-block;
            padding: 20px;
            background: white;
            border-radius: 15px;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.12);
        }

        .copy-link-btn {
            margin-top: 25px;
            padding: 16px 40px;
            background: linear-gradient(135deg, #ff6b35 0%, #ff8c42 100%);
            color: white;
            border: none;
            border-radius: 35px;
            font-size: 1.3rem;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(255, 107, 53, 0.3);
            font-family: 'Poppins', sans-serif;
        }

        .copy-link-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(255, 107, 53, 0.5);
        }

        .copy-link-btn:active {
            transform: translateY(-1px);
        }

        .toast {
            position: fixed;
            top: 25px;
            right: 25px;
            background: linear-gradient(135deg, #10b981 0%, #059669 100%);
            color: white;
            padding: 18px 32px;
            border-radius: 12px;
            box-shadow: 0 8px 25px rgba(16, 185, 129, 0.4);
            font-weight: 700;
            font-size: 1.2rem;
            z-index: 1000;
            opacity: 0;
            transform: translateX(400px);
            transition: all 0.4s ease;
        }

        .toast.show {
            opacity: 1;
            transform: translateX(0);
        }

        .receipt-footer {
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            padding: 35px 30px;
            text-align: center;
            border-top: 4px solid #ff6b35;
        }

        .agent-info {
            font-size: 1.4rem;
            color: #1e3a5f;
            margin-bottom: 12px;
            font-weight: 600;
        }

        .agent-name {
            font-weight: 800;
            color: #ff6b35;
            font-size: 1.6rem;
        }

        .footer-text {
            font-size: 1.15rem;
            color: #666;
            margin-top: 20px;
            line-height: 1.7;
            font-weight: 500;
        }

        /* Mobile Optimizations - SIGNIFICANTLY LARGER FONTS */
        @media (max-width: 768px) {
            body {
                padding: 20px 12px;
                font-size: 17px;
            }

            .receipt-header {
                padding: 35px 25px;
            }

            .receipt-header h1 {
                font-size: 2.5rem;
            }

            .receipt-number {
                font-size: 1.4rem;
            }

            .receipt-body {
                padding: 35px 25px;
            }

            .info-section {
                padding: 25px;
                margin-bottom: 35px;
            }

            .info-label {
                font-size: 1.3rem;
            }

            .info-value {
                font-size: 1.3rem;
                margin-top: 8px;
            }

            .info-row {
                flex-direction: column;
                align-items: flex-start;
                gap: 10px;
                padding: 16px 0;
            }

            .section-title {
                font-size: 1.85rem;
            }

            .ticket-card {
                padding: 25px;
            }

            .ticket-type {
                font-size: 1.4rem;
            }

            .ticket-details {
                grid-template-columns: 1fr;
                gap: 16px;
            }

            .detail-label {
                font-size: 1.05rem;
            }

            .detail-value {
                font-size: 1.35rem;
            }

            .numbers-label {
                font-size: 1.35rem;
            }

            /* 🎯 MOBILE TICKET NUMBERS - SUPER VISIBLE */
            .ticket-number {
                font-size: 2.2rem;
                font-weight: 900;
                padding: 22px 32px;
                min-width: 95px;
                letter-spacing: 2px;
            }

            .ticket-card.winner .ticket-number {
                font-size: 2.4rem;
            }

            .qr-section {
                padding: 30px 25px;
            }

            .qr-title {
                font-size: 1.5rem;
            }

            .copy-link-btn {
                font-size: 1.25rem;
                padding: 15px 35px;
            }

            .receipt-footer {
                padding: 30px 25px;
            }

            .agent-info {
                font-size: 1.3rem;
            }

            .agent-name {
                font-size: 1.5rem;
            }

            .footer-text {
                font-size: 1.1rem;
            }

            .toast {
                right: 15px;
                top: 15px;
                font-size: 1.15rem;
                padding: 16px 28px;
            }
        }

        /* Extra small devices - MAXIMUM READABILITY */
        @media (max-width: 480px) {
            body {
                font-size: 16px;
            }

            .receipt-header h1 {
                font-size: 2.2rem;
            }

            .receipt-number {
                font-size: 1.3rem;
            }

            .info-label,
            .info-value {
                font-size: 1.25rem;
            }

            .section-title {
                font-size: 1.75rem;
            }

            .ticket-type {
                font-size: 1.3rem;
            }

            .detail-value {
                font-size: 1.3rem;
            }

            /* 🎯 SMALL MOBILE - TICKET NUMBERS DOMINATE */
            .ticket-number {
                font-size: 2.4rem;
                font-weight: 900;
                padding: 24px 36px;
                min-width: 100px;
                letter-spacing: 2px;
            }

            .ticket-card.winner .ticket-number {
                font-size: 2.6rem;
            }

            .numbers-grid {
                gap: 12px;
                justify-content: center;
            }
        }

        /* Large devices - BOLD AND CLEAR */
        @media (min-width: 1024px) {
            body {
                font-size: 19px;
            }

            .receipt-header h1 {
                font-size: 3.5rem;
            }

            .receipt-number {
                font-size: 1.7rem;
            }

            .info-label,
            .info-value {
                font-size: 1.5rem;
            }

            .section-title {
                font-size: 2.2rem;
            }

            .ticket-type {
                font-size: 1.6rem;
            }

            .detail-label {
                font-size: 1.15rem;
            }

            .detail-value {
                font-size: 1.5rem;
            }

            .numbers-label {
                font-size: 1.5rem;
            }

            /* 🎯 DESKTOP - TICKET NUMBERS COMMAND ATTENTION */
            .ticket-number {
                font-size: 2.3rem;
                font-weight: 900;
                padding: 22px 32px;
                min-width: 95px;
            }

            .ticket-card.winner .ticket-number {
                font-size: 2.5rem;
            }

            .qr-title {
                font-size: 1.75rem;
            }

            .copy-link-btn {
                font-size: 1.4rem;
                padding: 18px 45px;
            }

            .agent-info {
                font-size: 1.5rem;
            }

            .agent-name {
                font-size: 1.7rem;
            }

            .footer-text {
                font-size: 1.2rem;
            }
        }

        /* Ultra-wide screens */
        @media (min-width: 1440px) {
            .receipt-header h1 {
                font-size: 4rem;
            }

            .ticket-number {
                font-size: 2.5rem;
                padding: 24px 36px;
            }

            .ticket-card.winner .ticket-number {
                font-size: 2.7rem;
            }
        }

        @media print {
            body {
                background: white;
                padding: 0;
            }

            .receipt-container {
                box-shadow: none;
                max-width: 100%;
            }

            .copy-link-btn {
                display: none;
            }

            .ticket-card:hover {
                transform: none;
            }
        }
    </style>
</head>
<body>
    <div class="receipt-container">
        <div class="receipt-header">
            <h1>🎟️ Lottery Receipt</h1>
            <div class="receipt-number">Receipt #<%= receiptData.receiptNumber %></div>
        </div>

        <div class="receipt-body">
            <div class="info-section">
                <div class="info-row">
                    <span class="info-label">📅 Date</span>
                    <span class="info-value"><%= receiptData.formattedDate %></span>
                </div>
                <div class="info-row">
                    <span class="info-label">⏰ Time</span>
                    <span class="info-value"><%= receiptData.formattedTime %></span>
                </div>
                <div class="info-row">
                    <span class="info-label">👤 Customer Name</span>
                    <span class="info-value"><%= receiptData.customerName %></span>
                </div>
                <div class="info-row">
                    <span class="info-label">📱 Phone</span>
                    <span class="info-value"><%= receiptData.phone %></span>
                </div>
                <div class="info-row">
                    <span class="info-label">🎫 Total Tickets</span>
                    <span class="info-value"><%= receiptData.totalTickets %></span>
                </div>
                <div class="info-row">
                    <span class="info-label">💰 Total Amount</span>
                    <span class="info-value">₹<%= receiptData.totalAmount.toFixed(2) %></span>
                </div>
            </div>

            <div class="tickets-section">
                <h2 class="section-title">🎲 Ticket Details</h2>
                <% receiptData.tickets.forEach((ticket, index) => { %>
                    <div class="ticket-card <%= ticket.isWon ? 'winner' : '' %>">
                        <div class="ticket-header">
                            <span class="ticket-type"><%= ticket.lotteryType %></span>
                            <% if (ticket.isWon) { %>
                                <span class="winner-badge">✨ Winner ✨</span>
                            <% } %>
                        </div>
                        <div class="ticket-details">
                            <div class="detail-item">
                                <span class="detail-label">Ticket ID</span>
                                <span class="detail-value">#<%= ticket.ticketId %></span>
                            </div>
                            <div class="detail-item">
                                <span class="detail-label">Draw Date</span>
                                <span class="detail-value"><%= ticket.drawDate %></span>
                            </div>
                            <div class="detail-item">
                                <span class="detail-label">Price</span>
                                <span class="detail-value">₹<%= ticket.price.toFixed(2) %></span>
                            </div>
                        </div>
                        <div class="ticket-numbers">
                            <div class="numbers-label">🎯 Selected Numbers</div>
                            <div class="numbers-grid">
                                <% ticket.numbers.forEach(number => { %>
                                    <span class="ticket-number"><%= number %></span>
                                <% }); %>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>

            <div class="qr-section">
                <div class="qr-title">📱 Scan to View Receipt</div>
                <div id="qrcode"></div>
                <button class="copy-link-btn" onclick="copyReceiptLink()">
                    📋 Copy Receipt Link
                </button>
            </div>
        </div>

        <div class="receipt-footer">
            <div class="agent-info">
                Sold by Agent: <span class="agent-name"><%= receiptData.agentName %></span>
            </div>
            <div class="footer-text">
                Thank you for your purchase! Keep this receipt safe for verification.<br>
                For queries, please contact your lottery agent.
            </div>
        </div>
    </div>

    <div class="toast" id="toast">Link copied to clipboard!</div>

    <script>
        // Generate QR Code
        const receiptUrl = window.location.href;
        new QRCode(document.getElementById("qrcode"), {
            text: receiptUrl,
            width: 200,
            height: 200,
            colorDark: "#1e3a5f",
            colorLight: "#ffffff",
            correctLevel: QRCode.CorrectLevel.H
        });

        // Copy link functionality
        function copyReceiptLink() {
            navigator.clipboard.writeText(receiptUrl).then(() => {
                const toast = document.getElementById('toast');
                toast.classList.add('show');
                setTimeout(() => {
                    toast.classList.remove('show');
                }, 3000);
            }).catch(err => {
                console.error('Failed to copy:', err);
                alert('Failed to copy link. Please copy manually: ' + receiptUrl);
            });
        }
    </script>
</body>
</html>